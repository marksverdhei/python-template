name: Prek
on:
  push:
    branches: main
  pull_request:
    branches: "*"

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: j178/prek-action@v1
        with:
          extra-args: '--all-files'
